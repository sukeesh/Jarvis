from plugin import plugin
import csv


@plugin("read_agenda")
def agenda(jarvis, s):
    try:
        f = open('agenda.csv', 'r')
        reader = csv.reader(f)
        for row in reader:
            print (row)
        f.close()
    except:
        print('There is not an agenda')
